{
  "name": "seoul_darak",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "next": "15.2.4",
    "nodemailer": "^6.10.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  },
  "homepage": "https://kimcj4567.github.io/seoul_darak/",
  "scripts": {
    "build": "next build && next export",
    "predeploy": "npm run build",
    "deploy": "touch out/.nojekyll && gh-pages -d out --dotfiles"
  }
}
